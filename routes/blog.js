const ZOOMY_ARTICLE = {
    id: 999001,
    title: "إطلاق نموذج Zoomy AI: النموذج الذكي الخاص والحيادي المطور كلياً بواسطة مهندسي منصة ZoomDz",
    slug: "zoomy-ai-model-announcement",
    excerpt: "تفخر منصة ZoomDz التعليمية بإطلاق نموذج المعلم الذكي Zoomy AI الخاص بها، والمطور كلياً بواسطة فريق مهندسي وباحثي المنصة للنهوض بالتعليم التفاعلي في الجزائر.",
    content: `<div style="line-height: 1.8; font-family: 'Cairo', sans-serif; font-size: 1.05rem; color: #334155;">
        <p style="font-size: 1.15rem; font-weight: 700; color: #1e293b; margin-bottom: 20px;">
            في إطار حرص منصة <strong>ZoomDz</strong> على تقديم أحدث التقنيات التعليمية وأعلاها دقة للمتعلمين في الجزائر، يسعدنا الإعلان رسمياً عن إطلاق نموذج <strong>Zoomy AI</strong> المتطور والخاص حصرياً بمنصتنا.
        </p>

        <div style="background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%); border-right: 5px solid #2563eb; padding: 20px; border-radius: 12px; margin: 25px 0;">
            <h3 style="margin-top:0; color: #1e40af; font-size: 1.2rem;"><i class="fas fa-brain"></i> نموذج ذكاء اصطناعي سيادي ومستقل</h3>
            <p style="margin-bottom:0;">
                نموذج <strong>Zoomy AI</strong> هو نموذج ذكاء اصطناعي محلي تمت هندسته وبرمجته وتطويره كلياً بواسطة مهندسي وباحثي منصة <strong>ZoomDz</strong>. النموذج غير تابع لأي محركات خارجية أو شركات أجنبية مثل Gemini أو غيرها، بل هو محرك خاص ومستقل مصمم ومدرّب خصيصاً لتلبية احتياجات المنظومة التربوية والمنهاج التعليمي الجزائري.
            </p>
        </div>

        <h3 style="color: #0f172a; border-bottom: 2px solid #e2e8f0; padding-bottom: 8px; margin-top: 30px;">
            🚀 أبرز قدرات ومميزات نموذج Zoomy AI:
        </h3>

        <ul style="list-style-type: none; padding-right: 0; line-height: 2;">
            <li style="margin-bottom: 12px; display: flex; align-items: flex-start; gap: 10px;">
                <span style="background: #2563eb; color: white; width: 26px; height: 26px; border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; font-size: 0.85rem; flex-shrink: 0; margin-top: 2px;">✓</span>
                <div><strong>استيعاب دقيق للمناهج الجزائرية (BEM / BAC):</strong> تم تدريب Zoomy AI على المقررات الرسمية والامتحانات والتمارين النموذجية لجميع المراحل الدراسية.</div>
            </li>
            <li style="margin-bottom: 12px; display: flex; align-items: flex-start; gap: 10px;">
                <span style="background: #2563eb; color: white; width: 26px; height: 26px; border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; font-size: 0.85rem; flex-shrink: 0; margin-top: 2px;">✓</span>
                <div><strong>تحليل ومعالجة الصور المتقدمة (Vision OCR):</strong> استخراج المسائل والتمارين من الصور الملتقطة بالكاميرا وحلها خطوة بخطوة بأسلوب أكاديمي دقيق.</div>
            </li>
            <li style="margin-bottom: 12px; display: flex; align-items: flex-start; gap: 10px;">
                <span style="background: #2563eb; color: white; width: 26px; height: 26px; border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; font-size: 0.85rem; flex-shrink: 0; margin-top: 2px;">✓</span>
                <div><strong>مرونة لغوية فائقة:</strong> يتفاعل النموذج بنفس لغة المستخدم بدقة متناهية، سواء بالفصحى، الفرنسية، الإنجليزية، أو حتى بالدارجة الجزائرية.</div>
            </li>
            <li style="margin-bottom: 12px; display: flex; align-items: flex-start; gap: 10px;">
                <span style="background: #2563eb; color: white; width: 26px; height: 26px; border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; font-size: 0.85rem; flex-shrink: 0; margin-top: 2px;">✓</span>
                <div><strong>حيادية واستقلالية تامة:</strong> لا ينتمي النموذج لأي جهات خارجية ويكرس كل إمكانياته لخدمة طلاب وأساتذة منصة ZoomDz عبر الـ 58 ولاية.</div>
            </li>
        </ul>

        <div style="background: #f8fafc; border: 1px solid #e2e8f0; padding: 20px; border-radius: 12px; margin-top: 30px; text-align: center;">
            <h4 style="margin-top: 0; color: #1e293b;">جرب المعلم الذكي Zoomy AI الآن!</h4>
            <p style="color: #64748b; font-size: 0.95rem;">يمكنك التواصل مع Zoomy AI عبر تبويب "المعلم الذكي وحل التمارين" في لوحة الطالب، أو عبر أيقونة المساعد الذكي المتاحة في صفحات المنصة.</p>
        </div>
    </div>`,
    cover_image: "https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?w=1200&q=80",
    seo_keywords: "Zoomy AI, منصة ZoomDz, ذكاء اصطناعي تعليمي, الجزائر, BEM, BAC",
    meta_description: "إطلاق نموذج Zoomy AI الخاص بمنصة ZoomDz للذكاء الاصطناعي التعليمي في الجزائر.",
    author: "فريق هندسة منصة ZoomDz",
    created_at: "2026-08-27T00:00:00.000Z"
};

const express = require('express');
const router = express.Router();
const { supabase } = require('../config/database');
const { authenticate, authorize } = require('../middleware/auth');
const logger = require('../utils/logger');

// Helper function to format Imgur URLs into direct image links
function formatImgurUrl(url) {
    if (!url || typeof url !== 'string') return '';
    let trimmed = url.trim();
    if (!trimmed) return '';

    // If protocol is missing, prepend https://
    if (!trimmed.startsWith('http://') && !trimmed.startsWith('https://')) {
        if (trimmed.includes('imgur.com')) {
            trimmed = 'https://' + trimmed;
        }
    }

    // Strip query parameters and hashes
    trimmed = trimmed.split('?')[0].split('#')[0];

    // Handle Imgur URLs
    if (trimmed.includes('imgur.com')) {
        // Direct i.imgur.com link
        if (trimmed.includes('i.imgur.com')) {
            if (!/\.(png|jpg|jpeg|gif|webp)$/i.test(trimmed)) {
                trimmed += '.png';
            }
            return trimmed;
        }

        // Match gallery, album, or direct image hash
        const match = trimmed.match(/imgur\.com\/(?:a\/|gallery\/|r\/[a-zA-Z0-9_-]+\/)?([a-zA-Z0-9]+)/i);
        if (match && match[1]) {
            return `https://i.imgur.com/${match[1]}.png`;
        }
    }
    return trimmed;
}

// Get all published blog posts
router.get('/blogs', async (req, res) => {
    try {
        let blogs = [];
        const { data, error } = await supabase
            .from('blogs')
            .select('*')
            .order('created_at', { ascending: false });

        if (!error && data) {
            blogs = data;
        }

        // Always prepend the Zoomy AI announcement article if not present
        const hasZoomy = blogs.some(b => b.slug === ZOOMY_ARTICLE.slug || b.id === ZOOMY_ARTICLE.id);
        if (!hasZoomy) {
            blogs.unshift(ZOOMY_ARTICLE);
        }

        res.json({ success: true, blogs });
    } catch (e) {
        logger.error('Error fetching blogs:', e);
        res.json({ success: true, blogs: [ZOOMY_ARTICLE] });
    }
});

// Get single blog post by slug or id
router.get('/blogs/:identifier', async (req, res) => {
    try {
        const identifier = decodeURIComponent(req.params.identifier);
        
        if (identifier === ZOOMY_ARTICLE.slug || identifier === String(ZOOMY_ARTICLE.id) || identifier === ZOOMY_ARTICLE.title) {
            return res.json({ success: true, blog: ZOOMY_ARTICLE });
        }

        let { data, error } = await supabase
            .from('blogs')
            .select('*')
            .eq('slug', identifier)
            .maybeSingle();

        if (!data && !isNaN(identifier) && Number.isInteger(Number(identifier))) {
            const idRes = await supabase
                .from('blogs')
                .select('*')
                .eq('id', parseInt(identifier))
                .maybeSingle();
            data = idRes.data;
            error = idRes.error;
        }

        if (!data) {
            if (identifier.includes('zoomy') || identifier.includes('Zoomy') || identifier.includes('ai')) {
                return res.json({ success: true, blog: ZOOMY_ARTICLE });
            }
            return res.status(404).json({ success: false, error: 'المقال غير موجود' });
        }

        res.json({ success: true, blog: data });
    } catch (e) {
        logger.error('Error fetching single blog:', e);
        res.json({ success: true, blog: ZOOMY_ARTICLE });
    }
});

// Admin: Create blog post
router.post('/admin/blogs', authenticate, authorize(['admin']), async (req, res) => {
    try {
        const { title, slug, excerpt, content, cover_image, seo_keywords, meta_description, author } = req.body;

        if (!title || !content) {
            return res.status(400).json({ success: false, error: 'العنوان ومحتوى المقال مطلوبان' });
        }

        const formattedCoverImage = formatImgurUrl(cover_image);
        const finalSlug = slug ? slug.trim().toLowerCase().replace(/\s+/g, '-') : title.trim().toLowerCase().replace(/[^\w\s-]/g, '').replace(/\s+/g, '-');

        const newBlog = {
            title,
            slug: finalSlug,
            excerpt: excerpt || '',
            content,
            cover_image: formattedCoverImage,
            seo_keywords: seo_keywords || '',
            meta_description: meta_description || excerpt || '',
            author: author || 'إدارة ZoomDz',
            created_at: new Date().toISOString()
        };

        const { data, error } = await supabase
            .from('blogs')
            .insert([newBlog])
            .select()
            .single();

        if (error) {
            if (error.code === '42P01') {
                return res.status(400).json({ success: false, error: 'جدول المقالات (blogs) غير موجود في قاعدة البيانات' });
            }
            throw error;
        }

        res.json({ success: true, blog: data });
    } catch (e) {
        logger.error('Error creating blog:', e);
        res.status(500).json({ success: false, error: e.message || 'Internal server error' });
    }
});

// Admin: Update existing blog post
router.put('/admin/blogs/:id', authenticate, authorize(['admin']), async (req, res) => {
    try {
        const blogId = req.params.id;
        const { title, slug, excerpt, content, cover_image, seo_keywords, meta_description, author } = req.body;

        if (!title || !content) {
            return res.status(400).json({ success: false, error: 'العنوان ومحتوى المقال مطلوبان' });
        }

        const formattedCoverImage = formatImgurUrl(cover_image);
        const finalSlug = slug ? slug.trim().toLowerCase().replace(/\s+/g, '-') : title.trim().toLowerCase().replace(/[^\w\s-]/g, '').replace(/\s+/g, '-');

        const updatedData = {
            title,
            slug: finalSlug,
            excerpt: excerpt || '',
            content,
            cover_image: formattedCoverImage,
            seo_keywords: seo_keywords || '',
            meta_description: meta_description || excerpt || '',
            updated_at: new Date().toISOString()
        };

        if (author) updatedData.author = author;

        const { data, error } = await supabase
            .from('blogs')
            .update(updatedData)
            .eq('id', blogId)
            .select()
            .single();

        if (error) throw error;

        res.json({ success: true, blog: data, message: 'تم تحديث المقال بنجاح' });
    } catch (e) {
        logger.error('Error updating blog:', e);
        res.status(500).json({ success: false, error: e.message || 'Internal server error' });
    }
});

// Admin: Delete blog post
router.delete('/admin/blogs/:id', authenticate, authorize(['admin']), async (req, res) => {
    try {
        const blogId = req.params.id;
        const { error } = await supabase
            .from('blogs')
            .delete()
            .eq('id', blogId);

        if (error) throw error;

        res.json({ success: true, message: 'تم حذف المقال بنجاح' });
    } catch (e) {
        logger.error('Error deleting blog:', e);
        res.status(500).json({ success: false, error: 'Internal server error' });
    }
});

module.exports = router;
